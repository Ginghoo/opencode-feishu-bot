# 飞书应用凭证（从 https://open.feishu.cn/app 获取）
FEISHU_APP_ID=cli_xxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 管理员用户 ID（逗号分隔的 open_id）
# 这些用户可以管理白名单并拥有完全访问权限
ADMIN_USER_IDS=ou_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 是否允许所有用户使用（默认 true）
# 设为 false 则启用白名单模式，只有管理员和白名单用户可以使用
ALLOW_ALL_USERS=true

# 数据库配置
# SQLite 数据库文件存储路径
DATABASE_PATH=./data/bot.db

# 日志级别（debug | info | warn | error）
LOG_LEVEL=info

# 注意：OpenCode 会自动启动，无需手动配置
# 默认项目目录为机器人启动时的当前工作目录

# 预配置项目列表（用于菜单快速切换）
# 格式：路径:显示名称,路径:显示名称
# 例如：PROJECTS=/home/user/project-a:项目A,/home/user/project-b:项目B
PROJECTS=

# 可用模型列表（用于欢迎卡片和菜单中的模型选择）
# 留空则显示 OpenCode 返回的全部模型
# 格式：模型ID 或 模型ID:显示名称（逗号分隔多个模型）
# 注意：使用最后一个冒号分隔 ID 和名称，模型 ID 中可包含冒号
# 例如：AVAILABLE_MODELS=anthropic/claude-3-5-sonnet-20241022:Claude Sonnet,openai/gpt-4o:GPT-4o
AVAILABLE_MODELS=

# 默认模型（新会话自动使用此模型）
# 可被 CLI 参数 --model 覆盖
# 格式：provider/model
# 例如：DEFAULT_MODEL=anthropic/claude-sonnet-4-20250514
DEFAULT_MODEL=
